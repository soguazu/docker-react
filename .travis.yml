sudo: required

services:
  - docker

before_install:
  - docker build -t dearests/docker-react  -f Dockerfile.dev .

script:
  - docker run -e CI=true dearests/docker-react npm run test -- --coverage
